# v1.6.5

## Fixes

### 1. auto-cr detection
- Check binary `check` (the actual binary name)
- Use `--help` instead of `--version` for detection

### 2. --fix flag now works
- Actually sets CXXFLAGS for current session
- Adds to ~/.bashrc for future sessions
- Shows "Auto-configured!" message

### 3. CXXFLAGS properly set
- Sets `CXXFLAGS` and `npm_config_cxxflags` for current session
- Adds export to ~/.bashrc

---

## v1.6.4

### Auto-fix mode

```bash
bunx @hung319/opencode-hive doctor --fix
```
