# v1.6.1

## What's Changed

### Restored: ast-grep MCP

**ast_grep MCP is back!** (via `@notprolands/ast-grep-mcp`)

- MCP tool: `ast_grep_search` available via MCP
- Native tools (ast-grep-native) remain removed to avoid build issues
- Uses npx execution (works everywhere)

### Doctor checks updated

- Added back: `@ast-grep/napi`, `@notprolands/ast-grep-mcp`
- Added back: `ast-grep` CLI tool check

---

## v1.6.0

### Breaking: Removed ast-grep native tools

- Removed native ast-grep tools to fix build issues on Node.js v24
- ast-grep MCP still available

---

## v1.5.9

### NEW: Standalone Doctor

```bash
bunx @hung319/opencode-hive doctor
```
