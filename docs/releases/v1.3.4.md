# Release v1.3.3

## Bug Fixes

- Fix ConfigService test: expected agent list was not alphabetically sorted
- Skip readonly permission test when running as root (root bypasses permissions)
- Fixed `BUILT_IN_AGENT_NAMES` and `isPlannerAgent` to use `zetta` instead of `hive`

## Test Results

- All tests passing (409 pass, 10 skip, 0 fail)
- Build: Passed
