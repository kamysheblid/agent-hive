# v1.6.4

## What's Changed

### Auto-fix mode for doctor

```bash
bunx @hung319/opencode-hive doctor --fix
```

**Auto-fix will:**
1. Auto-add `CXXFLAGS="-std=c++20"` to `~/.bashrc`
2. Install CLI tools via npx

### Simplified doctor checks

- Only checks optional agent tools and CLI tools
- MCPs auto-installed with plugin (no need to check)
- C++20 status shown clearly

### Unified code

- Standalone doctor (`bin/doctor.ts`) and plugin tool (`hive_doctor.ts`) share similar logic
- Better JSON output for programmatic use

---

## v1.6.3

### Fix: colors.cyan and C++20 tip

- Add missing `colors.cyan` function
- Add tip about CXXFLAGS for tree-sitter

---

## v1.6.2

### Removed: ast-grep MCP

Use `@ast-grep/napi` directly instead.
