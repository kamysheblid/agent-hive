# v1.6.2

## What's Changed

### Removed: ast-grep MCP

**ast_grep MCP (via @notprolands/ast-grep-mcp) removed.**

Use `@ast-grep/napi` directly instead - it's already in dependencies and provides native NAPI-based ast-grep functionality.

### Doctor checks updated

- Removed: `@notprolands/ast-grep-mcp`
- Removed: `ast-grep` CLI tool check
- Kept: `@ast-grep/napi`

---

## v1.6.1

### Restored: ast-grep MCP

Added back @notprolands/ast-grep-mcp as optional dependency.

---

## v1.6.0

### Breaking: Removed ast-grep native tools

- Removed native ast-grep tools to fix build issues
- ast-grep MCP still available

---

## v1.5.9

### NEW: Standalone Doctor

```bash
bunx @hung319/opencode-hive doctor
```
