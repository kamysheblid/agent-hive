# v1.7.3

## Summary

TypeScript build fix - adds optional packages to devDependencies for type checking.

## Changes

- Added `@sparkleideas/agent-booster` and `@ast-grep/napi` to devDependencies
- Fixes `Cannot find module` error during `tsc --emitDeclarationOnly`

## Installation

```bash
npm install @hung319/opencode-hive@1.7.3
```

## Note

This is the same optimization as v1.7.2 (reduced from 384 to ~10 packages) but with build fix.
