# v1.15.1

## Features

- **MCP Strategy injected** into ALL agents: every agent now has a table listing available MCPs (websearch, context7, grep_app, repomix, crw, ast_grep) with tool names and use-cases
- **CRW**: Only requires `CRW_API_URL` now; `CRW_API_KEY` is optional

## Fixes

- **CRW MCP**: Conditional activation (only when `CRW_API_URL` is set)
- **Worker prompt**: Added batch-edits guideline to reduce round-trips

## Docs

- MCP guide updated with CRW section
- Scout tool strategy includes CRW tools
