{
  "mcpServers": {
    "grep_app": {
      "command": "npx",
      "args": ["-y", "grep-mcp"],
      "description": "GitHub code search via grep.app"
    },
    "ast_grep": {
      "command": "npx",
      "args": ["-y", "@notprolands/ast-grep-mcp"],
      "description": "AST-aware code search and refactoring (legacy MCP, consider using native tools)"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Library documentation lookup"
    },
    "websearch_exa": {
      "command": "npx",
      "args": ["-y", "exa-mcp-server"],
      "env": {
        "EXA_API_KEY": "${EXA_API_KEY}"
      },
      "description": "Exa AI web search (requires API key)"
    },
    "veil": {
      "command": "npx",
      "args": ["-y", "@ushiradineth/veil@latest", "mcp", "server"],
      "description": "Code discovery and retrieval for AI agents"
    }
  }
}
