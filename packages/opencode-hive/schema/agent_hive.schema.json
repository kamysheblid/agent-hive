{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/hung319/agent-hive/main/packages/opencode-hive/schema/agent_hive.schema.json",
  "title": "Agent Hive Configuration",
  "description": "Configuration schema for ~/.config/opencode/agent_hive.json",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Schema reference for this config file"
    },
    "sandbox": {
      "type": "string",
      "enum": ["none", "docker"],
      "default": "none",
      "description": "Sandbox isolation mode. 'none' runs commands directly; 'docker' wraps bash commands in Docker containers."
    },
    "dockerImage": {
      "type": "string",
      "description": "Docker image to use for sandbox. Auto-detected from project files if omitted (e.g., node:22-slim for Node.js projects)."
    },
    "persistentContainers": {
      "type": "boolean",
      "default": true,
      "description": "Reuse Docker containers per worktree (docker exec) instead of creating new ones per command (docker run --rm). Default: true when sandbox is 'docker'."
    },
    "enableToolsFor": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Enable hive tools for specific features"
    },
    "disableSkills": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Globally disable specific skills (e.g., ['brainstorming', 'writing-plans']). Disabled skills won't appear in hive_skill tool."
    },
    "disableMcps": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Globally disable specific MCP servers (e.g., ['websearch', 'ast_grep']). Available: websearch, context7, grep_app, ast_grep"
    },
    "agentMode": {
      "type": "string",
      "enum": [
        "unified",
        "dedicated"
      ],
      "default": "unified",
      "description": "Agent specialization mode. 'unified' (default) uses a single hybrid agent for all roles; 'dedicated' uses specialized agents for each role."
    },
    "hook_cadence": {
      "type": "object",
      "description": "Hook execution cadence configuration. Key = hook name, Value = number of turns between invocations (1 = every turn, 3 = every 3rd turn). All hooks default to 1.",
      "additionalProperties": {
        "type": "integer",
        "minimum": 1,
        "description": "Number of turns between hook invocations"
      },
      "examples": [
        {
          "experimental.chat.system.transform": 3,
          "chat.message": 1,
          "tool.execute.before": 1
        }
      ]
    },
    "contextCompression": {
      "type": "object",
      "description": "Context compression configuration (DCP-style automatic pruning)",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic context compression when threshold is reached"
        },
        "threshold": {
          "type": "number",
          "minimum": 0.1,
          "maximum": 0.9,
          "default": 0.5,
          "description": "Context usage threshold to trigger compression (0.5 = 50% of context limit)"
        },
        "maxToolCalls": {
          "type": "number",
          "minimum": 10,
          "maximum": 200,
          "default": 50,
          "description": "Maximum number of tool calls to keep after compression"
        },
        "protectedTools": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Tool names to protect from compression (glob patterns supported)"
        },
        "protectUserMessages": {
          "type": "boolean",
          "default": true,
          "description": "Protect user messages from being compressed"
        }
      }
    },
    "memory": {
      "type": "object",
      "description": "Memory and session summarization configuration",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable automatic session summarization and memory persistence"
        },
        "autoSummarize": {
          "type": "boolean",
          "default": true,
          "description": "Automatically summarize conversation after N messages"
        },
        "summarizeAfterMessages": {
          "type": "number",
          "minimum": 5,
          "maximum": 100,
          "default": 20,
          "description": "Number of messages after which to trigger summarization"
        },
        "useOpencodeDb": {
          "type": "boolean",
          "default": true,
          "description": "Store memories in OpenCode database path for cross-session persistence"
        }
      }
    },
    "mcpApiKeys": {
      "type": "object",
      "description": "MCP API keys configuration (also supports environment variables: EXA_API_KEY, CONTEXT7_API_KEY)",
      "properties": {
        "exa": {
          "type": "string",
          "description": "Exa API key for websearch (optional - works without it)"
        },
        "context7": {
          "type": "string",
          "description": "Context7 API key (optional - works without it)"
        }
      }
    },
    "agents": {
      "type": "object",
      "description": "Agent model configuration overrides",
      "additionalProperties": false,
      "properties": {
        "zetta": {
          "$ref": "#/$defs/agentConfig",
          "description": "Zetta (hybrid planner + orchestrator)"
        },
        "architect-planner": {
          "$ref": "#/$defs/agentConfig",
          "description": "Architect Planner (planning-only)"
        },
        "swarm-orchestrator": {
          "$ref": "#/$defs/agentConfig",
          "description": "Swarm Orchestrator"
        },
        "scout-researcher": {
          "$ref": "#/$defs/agentConfig",
          "description": "Scout Researcher"
        },
        "forager-worker": {
          "$ref": "#/$defs/agentConfig",
          "description": "Forager Worker"
        },
        "hygienic-reviewer": {
          "$ref": "#/$defs/agentConfig",
          "description": "Hygienic Reviewer"
        }
      }
    },
    "customAgents": {
      "type": "object",
      "description": "Custom Hive subagent definitions derived from forager-worker or hygienic-reviewer. IDs cannot reuse built-in or plugin-reserved names.",
      "propertyNames": {
        "not": {
          "enum": [
            "zetta",
            "architect-planner",
            "swarm-orchestrator",
            "scout-researcher",
            "forager-worker",
            "hygienic-reviewer",
            "architect",
            "swarm",
            "scout",
            "forager",
            "hygienic",
            "receiver",
            "build",
            "plan",
            "code"
          ]
        }
      },
      "additionalProperties": {
        "$ref": "#/$defs/customAgentConfig"
      }
    }
  },
  "$defs": {
    "agentConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "model": {
          "type": "string",
          "description": "Model to use (provider/model-id)"
        },
        "temperature": {
          "type": "number",
          "minimum": 0,
          "maximum": 2,
          "description": "Sampling temperature"
        },
        "top_p": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Top-p (nucleus) sampling — cumulative probability threshold (0–1)"
        },
        "top_k": {
          "type": "integer",
          "minimum": 0,
          "description": "Top-k sampling — limits to top K most likely tokens (positive integer)"
        },
        "min_p": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Minimum probability mass — only tokens with cumulative probability ≥ this threshold are considered (0–1)"
        },
        "repeat_penalty": {
          "type": "number",
          "minimum": 0,
          "description": "Repeat penalty — penalizes tokens that have already appeared in the prompt (≥ 0)"
        },
        "frequency_penalty": {
          "type": "number",
          "minimum": -2,
          "maximum": 2,
          "description": "Frequency penalty — reduces likelihood of tokens already present in the completion (-2 to 2)"
        },
        "presence_penalty": {
          "type": "number",
          "minimum": -2,
          "maximum": 2,
          "description": "Presence penalty — increases likelihood of tokens already present in the prompt (-2 to 2)"
        },
        "skills": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Skills enabled for this agent"
        },
        "autoLoadSkills": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Skills to auto-load for this agent"
        },
        "variant": {
          "type": "string",
          "description": "Variant key for model reasoning/effort level (e.g., 'low', 'medium', 'high', 'max'). Must match a key in opencode.json provider.<provider>.models.<model>.variants"
        },
        "customPrompt": {
          "type": "string",
          "description": "Additional prompt content injected into the agent's system prompt. Useful for giving a subagent a specific role or instructions."
        }
      }
    },
    "customAgentConfig": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "baseAgent",
        "description"
      ],
      "properties": {
        "baseAgent": {
          "type": "string",
          "enum": [
            "forager-worker",
            "hygienic-reviewer"
          ],
          "description": "Built-in Hive agent to inherit prompt/tools/permissions from"
        },
        "description": {
          "type": "string",
          "minLength": 1,
          "description": "Human-facing description injected into delegation guidance"
        },
        "model": {
          "type": "string",
          "description": "Model override (provider/model-id)"
        },
        "temperature": {
          "type": "number",
          "minimum": 0,
          "maximum": 2,
          "description": "Sampling temperature override"
        },
        "top_p": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Top-p (nucleus) sampling override — cumulative probability threshold (0–1)"
        },
        "top_k": {
          "type": "integer",
          "minimum": 0,
          "description": "Top-k sampling override — limits to top K most likely tokens (positive integer)"
        },
        "min_p": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Minimum probability mass override — only tokens with cumulative probability ≥ this threshold are considered (0–1)"
        },
        "repeat_penalty": {
          "type": "number",
          "minimum": 0,
          "description": "Repeat penalty override — penalizes tokens that have already appeared in the prompt (≥ 0)"
        },
        "frequency_penalty": {
          "type": "number",
          "minimum": -2,
          "maximum": 2,
          "description": "Frequency penalty override — reduces likelihood of tokens already present in the completion (-2 to 2)"
        },
        "presence_penalty": {
          "type": "number",
          "minimum": -2,
          "maximum": 2,
          "description": "Presence penalty override — increases likelihood of tokens already present in the prompt (-2 to 2)"
        },
        "variant": {
          "type": "string",
          "description": "Variant override for model reasoning/effort level"
        },
        "autoLoadSkills": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Additional skills to auto-load for this custom agent (merged with inherited base autoLoadSkills and filtered by disableSkills)"
        },
        "customPrompt": {
          "type": "string",
          "description": "Additional prompt content injected into the agent's system prompt. Useful for giving a custom subagent a specific role or instructions."
        }
      }
    }
  }
}
